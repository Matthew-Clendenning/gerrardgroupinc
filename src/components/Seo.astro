---
import jsonLDGenerator from "../scripts/jsonLD";

const {
  type = "website",
  data = {},
} = Astro.props;

const jsonLD = jsonLDGenerator({
  type,
  data,
  url: Astro.url?.href || "",
});
---

<!-- JSON LD -->
<Fragment set:html={jsonLD} />
